{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "agent-up/references/schemas/issue-index.schema.json",
  "title": "Agent Up issue status index schema",
  "description": "任务票状态索引（票状态机唯一真相源）机器 schema。落盘位置：docs/issues/index.json（终裁定稿，2026-09-18）。文件形态：单文件 JSON，一条目一行（终裁定稿：行可寻址，POSIX sh 零依赖脚本按 id 锚点整行替换）。写入 owner：快道启用时由道脚本独占写（对齐 development-process §12.5 派生载体独占写先例），非快道任务由对应阶段执行体单写。本索引是真相源（Record 类），不是 Derived；消费它的投影才是 Derived。",
  "type": "object",
  "additionalProperties": false,
  "required": ["issues"],
  "properties": {
    "issues": {
      "type": "array",
      "description": "任务票状态条目数组，每票恰一条目，id 唯一。落盘排版约束：单文件 JSON 一条目一行（行可寻址，POSIX sh 零依赖脚本可整行替换；终裁定稿）。",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "status", "complexity", "blocked_by", "updated_at"],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[0-9]{2,}-[a-z0-9-]+$",
            "description": "票文件名去扩展名（对齐 SPEC-02 §5.1 frontmatter id 口径）。"
          },
          "status": {
            "type": "string",
            "pattern": "^(ready|in_progress|blocked(:[ a-zA-Z0-9-]+)?|review_ready|review_pass|review_fail|done|superseded)$",
            "description": "Task 状态机取值（development-process §12.1）＋票据生命周期标签 `superseded`（语义权威＝docs/issues/README.md，仅用于索引登记历史票据，非 Task 流转取值）。"
          },
          "lane": {
            "type": "string",
            "enum": ["full", "user-review", "micro"],
            "description": "分级交付道车道（R-DP-031）；枚举定稿 full/user-review/micro（2026-09-18）；未声明车道可省略。"
          },
          "complexity": {
            "type": "string",
            "enum": ["C0", "C1", "C2", "C3"],
            "description": "复杂度判级（对齐 complexity-profile 权威表）。"
          },
          "blocked_by": {
            "type": "array",
            "items": { "type": "string", "pattern": "^[0-9]{2,}-[a-z0-9-]+$" },
            "description": "被依赖票 id 列表；无依赖为空数组（对齐 SPEC-02 §5.1 blocked_by 口径）。"
          },
          "checkpoint_ref": {
            "type": "string",
            "description": "最近检查点指针：<票路径>#<Checkpoint 锚点文本>；尚无检查点省略本字段。"
          },
          "last_verified": {
            "type": "object",
            "additionalProperties": false,
            "required": ["command", "result", "at"],
            "properties": {
              "command": { "type": "string", "minLength": 1 },
              "result": { "type": "string", "minLength": 1, "description": "真实结果；未运行不写本对象。" },
              "at": { "type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}", "description": "ISO 8601 日期或日期时间。" }
            },
            "description": "该票最后一次验证，口径同 run record（development-process §12.4）；未验证省略本字段。"
          },
          "updated_at": {
            "type": "string",
            "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}",
            "description": "条目最后更新时间，ISO 8601 日期时间。"
          }
        }
      }
    }
  }
}
