<!-- Input: 已确认的业务语义与核验结果。 -->
<!-- Output: 角色、对象、状态、流程和范围的统一领域语言。 -->
<!-- Pos: 领域事实权威（条件性产物）；一旦我被更新，务必更新我的开头注释，以及所属文件夹的 README.md。 -->
<!-- read_when: confirmed-domain-language —— 命中触发矩阵"确认了项目特有术语、角色或状态"才创建并读取；未命中不创建（触发矩阵见 docs/development-process.md）。 -->

# 领域上下文

本文件只描述业务含义，不描述技术实现。词汇格式遵循 domain-modeling 的 CONTEXT-FORMAT：加粗术语 + 1～2 句定义（描述它是什么，不是它做什么）+ `_Avoid:_` 近义词；只收本项目特有概念，通用编程概念不进。只登记用户明确给出或从可靠来源核验过的词汇；没有依据的写 `【待定：…】`，不编造业务事实。

## 角色

【按项目填写：每个角色一段，格式为 `**角色名**：职责与定位`。容易混用的近义词用 `_Avoid:_` 标注。】

## 核心对象

【按项目填写：业务名词及其定义，一个对象一段加粗开头。】

## 状态

【按项目填写：核心对象的生命周期状态和合法转换。】

## 流程

【按项目填写：主要业务路径，按用户可感知的顺序描述。】

## 范围边界

【按项目填写：当前阶段明确不做什么，避免范围漂移。】
