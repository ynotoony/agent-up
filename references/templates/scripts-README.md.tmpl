<!-- Input: 仓库共享验证脚本与 init 落位工具的用途、用法与落位条件（口径基准＝公开包 SKILL.md 主流程步骤 5 与 references/old-project.md §3 补缺表，逐件登记定稿）。 -->
<!-- Output: 脚本目录索引：逐件登记（用途/用法指针/落位条件）与共享 harness 约定。 -->
<!-- Pos: 共享验证 harness 索引；一旦我被更新，务必更新我的开头注释，以及所属文件夹的 README.md。 -->
<!-- read_when: conditional-scripts —— 命中触发矩阵"可重复验证需要沉淀为共享 harness"或 init 落位工具复制落位（scripts/ 目录创建即一并生成）才创建；未命中不创建（触发矩阵见 docs/development-process.md）。 -->

# 脚本（scripts）

## 架构

- 本目录是仓库共享验证 harness：可重复执行的验证以脚本形式沉淀在这里，全仓库复用；禁止在任务或执行体重写一次性验证脚本（规则见 `docs/development-process.md` 验证与记录）。
- 约定：脚本接受场景/票号等作为输入参数；断言结果落 JSON 或结构化输出；截图、日志等产物只作存档，不回流对话上下文。
- 新增脚本**当场**在本 README 登记；先找已有脚本，没有适用的才写新的。
- 落位登记口径（对下表全部"从公开包复制落位"的脚本）：逐件登记 generation manifest 与 `docs/agent/artifacts.yaml`（kind: script、lifecycle: Conditional、generated_from: `agent-up/scripts/<文件名>`）；落位与登记的完整口径以公开包 `SKILL.md` 主流程步骤 5 与 `references/old-project.md` §3 补缺表为准，三处（SKILL、old-project、本 README）不得出现第四种说法。
- 自检工具落位：包内 `test-record-layer.sh`（记录层回归 harness）随记录层工具一并复制落位——目标项目采用任务票体系（`docs/issues/` 目录创建）时与 `ticket-ops.sh` 同门控复制到 `scripts/`（缺省自测同目录包内脚本，须与其被测成员同目录在位），落位后按上方逐件登记口径登记。

## 目录清单

| 名字 | 地位 | 功能与用法 | 落位条件 |
| --- | --- | --- | --- |
| `README.md` | 目录索引 | harness 约定与脚本登记（本件）。 | `scripts/` 目录创建时一并生成（任一落位工具复制落位或首个共享验证沉淀触发）。 |
| `check-gates.sh` | 快道门禁核对器 | 分级交付道快道的只读门禁核对：工作区实际改动 ⊆ 白名单逐项比对＋按清单重跑验证命令并记录退出码。用法：`sh scripts/check-gates.sh [repo-root] <gates-file>`；详见公开包 `agent-up/scripts/README.md` 对应专节与脚本 `-h`。 | 用户确认启用分级交付道快道时才从 agent-up 公开包 `scripts/` 复制落位；未启用不落位（道脚本不可用的停止条件按 `docs/development-process.md` R-DP-031 兜底）。 |
| `lane-commit.sh` | 快道收尾脚本 | 合同驱动收尾（两段式，fail-closed）：门禁 → 白名单产品提交 → `docs/issues/index.json` 索引单写 → 票正文 Status 投影打印件回写 → User Review Checkpoint 或微账本落行 → 投影再生并 `--check` 核对 → 记录提交。用法：`sh scripts/lane-commit.sh [repo-root] <contract-file>`；指针同上。 | 同 `check-gates.sh`（快道启用门控，与道脚本一并复制落位）。 |
| `generate-progress.sh` | 现役状态投影生成器 | 自 `docs/issues/index.json`（票状态真相源，一条目一行）生成 `docs/progress-current.md` 现役状态投影（生成器独占写，agent 不手写）；`--check` 为 dry-run 一致性核对；快道收尾（lane-commit）与票务运维收尾（ticket-ops）自动调用。用法：`sh scripts/generate-progress.sh [--check] [repo-root]`；指针同上。 | 双通道：快道启用时随道脚本一并复制落位；未启用快道但采用任务票体系时，作为 `ticket-ops.sh` 收尾投影再生依赖一并落位（未落位生成器时随票务脚本一并复制）。 |
| `ticket-ops.sh` | 票务运维脚本 | 协调层票务面单入口：`open`／`take`／`flip` 三子命令，`docs/issues/index.json` 单写、issues-README 状态列登记、`docs/changes.jsonl` 账本落行、收尾投影再生并 `--check` 核对（校验先于写入，fail-closed）。用法：`sh scripts/ticket-ops.sh [repo-root] <command> [options]`；指针同上。 | 记录层标配，不门控于快道启用：目标项目采用任务票体系（`docs/issues/` 目录创建）时随初始化/补缺复制落位；未采用任务票体系不落位。 |
| `generate-module-map.sh` | 模块地图生成器 | 静态导入行提取生成 `docs/architecture/module-map.json` 检索索引（Derived，只当检索索引不当 Scope 权威；fp-v1 工作区指纹内嵌，stale 即不得当可信导航）。用法：`sh scripts/generate-module-map.sh [repo-root]`；指针同上。 | 项目地图触发行命中时才复制落位（`docs/development-process.md` 触发矩阵行"项目地图"）；地图实例不预建，由生成器运行时懒创建。 |
| `module-map.rules` | 模块地图语言规则表 | `generate-module-map.sh` 的语言提取规则唯一承载点（每语言一行）；规则表缺失或不合预期时引擎 fail-closed 停止。 | 随 `generate-module-map.sh` 同目录落位，两件同迁、缺一不可。 |
| `<name>.<ext>` | 验证脚本 | 一行说明用途、输入与输出位置（共享验证沉淀登记形态）。 | 仓库内按 harness 纪律沉淀时新增并当场登记。 |
